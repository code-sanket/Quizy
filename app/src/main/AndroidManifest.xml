<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoActionBar">
        <activity android:name=".quizy.activities.ProfileActivity"></activity>
        <activity android:name=".quizy.activities.ResultActivity" />
        <activity
            android:name=".quizy.activities.QuestionActivity"
            android:theme="@style/Theme.Quizy">

        </activity>
        <activity android:name=".quizy.activities.SignupActivity" />
        <activity android:name=".quizy.activities.LoginIntro">

        </activity>
        <activity android:name=".quizy.activities.LoginActivity" />
        <activity android:name=".quizy.activities.MainActivity">
        </activity>
        <activity android:name=".quizy.activities.SplashScreen"
            android:theme="@style/SplashScreenBackground">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>